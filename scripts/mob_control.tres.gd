
extends RigidBody2D

# member variables here, example:
# var a=2
# var b="textvar"

# signal flicker_light

func _ready():
	pass
	# self.set_process(true)
	# connect("moved", self, "Camera2D")
	# var cam = get_node("Camera2D")
	# cam.connect("moved", self, "sprite_moved")

func _draw():
	pass
		# var pos = Vector2(self.get_parent().get_rect().size.width/2, self.get_parent().get_rect().size.height/2)

#func _process(delta):
	
#	self.emit_signal("flicker_light")
#		velocity = Vector2(rand_range(-150, 150), rand_range(-150, 150))
	# count += 0.5
	# print(velocity.x * delta)
	# print(velocity.y * delta)
	# var xpos = self.get_pos().x + velocity.x * delta
	# var ypos = self.get_pos().y + velocity.y * delta
	# self.set_pos(Vector2(xpos, ypos))
	# print(self.get_pos())
	#print(pos, self.get_pos())
	# self.emit_signal("moved")
	# self._draw()
	# pass # replace with function body
